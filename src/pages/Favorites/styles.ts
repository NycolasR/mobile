import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#f0f0f7',
    },

    // Estilização da scroll list de componentes da tela
    teacherList: {
        marginTop: -40, // Faz com que a lista se sobreponha ao cabeçalho
    },
});

export default styles;